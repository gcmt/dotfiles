
export MONITOR_PRIMARY="$(xrandr | grep "primary" | grep -o "^[^ ]\+")"

export QT_QPA_PLATFORMTHEME=qt5ct

xrdb -load ~/.Xresources

setxkbmap -layout it

xmodmap ~/.Xmodmap

xset s off dpms 0 0 0

xset r rate 175 25

exec i3
