
export QT_QPA_PLATFORMTHEME=qt5ct

xrdb -load ~/.Xresources

setxkbmap -layout it

xmodmap ~/.Xmodmap

xset r rate 175 25

sh ~/.config/dunst/start.sh &

compton -b

nitrogen --restore &

[ ! -s ~/.local/share/mpd/pid ] && mpd

tmux new -ds scratch 2>/dev/null

exec i3
